# Discord bot credentials from the Discord developer portal.
DISCORD_TOKEN=
DISCORD_APP_ID=
# Optional: register commands instantly in one server while developing.
DISCORD_GUILD_ID=
# Tokito API and dashboard.
API_URL=http://localhost:3002
DASHBOARD_URL=http://localhost:3000
# OpenAI for the natural-language agent (same as the API).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
# Must match API_TOKEN on the API when that is set.
API_TOKEN=
