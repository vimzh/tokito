# Tokito web

Next.js 16 app. See the root [README](../../README.md) for setup, environment variables, and the API it talks to.

- Copy for every screen lives in `src/data`.
- `src/lib/api.ts` is the typed client for the API; server components and server actions use it.
- Design and component rules are in `AGENTS.md`.
